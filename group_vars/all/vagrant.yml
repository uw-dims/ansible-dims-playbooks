---

# TODO(dittrich): Remove once we switch to using dictionary below.
threeD: 'off'
CPUs: 2
memory: 2048
vm_dir: '/vm'
vmUser: 'ansible'
vmKey: 'dims_ansible_rsa'

vm:
  threeD: 'off'
  CPUs: 2
  memory: 2048
  root: '/vm'
  vmUser: 'ansible'
  vmKey: 'dims_ansible_rsa'


# vim: ft=ansible :
