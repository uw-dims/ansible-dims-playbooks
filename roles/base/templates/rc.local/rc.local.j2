#!/bin/sh -e
#
# {{ ansible_managed }} [ansible-playbooks v{{ ansibleplaybooks_version }}]
#
# rc.local
#
# This script is executed at the end of each multiuser runlevel.
# Make sure that the script will "exit 0" on success or any other
# value on error.
#
# In order to enable or disable this script just change the execution
# bits.

# We want hostname to *always* be the short name.

hostname "{{ ansible_hostname }}"

# Force domainname to be set the simple way. We do not want to use
# fully qualified domain name for /etc/hostname, since some DIMS
# systems are multi-homed and it gets confusing having domain
# names show up that are not accessible because they are on an
# different interface (e.g., a VLAN, or overlay network).

domainname "{{ category }}.{{ deployment }}"

exit 0
