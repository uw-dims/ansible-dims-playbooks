These are files formatted like hosts files because
that's how dnsmasq gets its mappings (as opposed to
bind9 which uses its own database for mappings).

The names here are "hosts." followed by names that
differentiate specific zones that are being served.
