Following the model set by the Fedora Project:

   "this dir should only be filled with host-specific playbooks in the
    format of:

    fqdn.yml

    so we can easily know which hosts have a specific config vs a
    group config"

